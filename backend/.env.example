DATABASE_URL=postgres://chef_stol:chef_stol@localhost:5432/chef_stol?sslmode=disable
HTTP_ADDR=:8080
SESSION_TTL_HOURS=720

# Push (FEAT-02..05). Dev-only placeholders committed here on purpose so the frontend engineer's
# hardcoded VAPID public key matches what this backend signs with — DO NOT reuse this exact key
# pair or token in any real deployment, generate fresh ones instead.
#
# This pair was already rotated once (see git history around the "full project review" commit):
# the previous pair got committed to what is now a public repo and had to be treated as
# permanently exposed. Since this file lives in a public repo, treat this pair the same way if it
# needs replacing again — rotate with `npx web-push generate-vapid-keys` and update
# client/src/core/config.ts to match, the two must always be the same pair.
VAPID_PUBLIC_KEY=BI_GG7M7ixlJflnMuhL-dM2xgmH7k_SCB2TwEb3NhnVcaL37mmL1ibRWp6F4sxU4cYw-563a8fsG-qEooA31dSg
VAPID_PRIVATE_KEY=pQ8r02gUxiQGXNlGkbtkP9bcMIHqrifnvvC1m-VOpIA
VAPID_SUBJECT=mailto:dev@chef-stol.example
INTERNAL_TOOLS_TOKEN=dev-internal-tools-token-change-me
